#!/usr/bin/env bash
# Host convenience wrapper: regenerate the committed EasyUI golden fixtures
# (tests/fixtures/easyui/*.json) inside the running simplerisk-app container.
#
# Run this AFTER an intentional change to an EasyUI tree/treegrid endpoint's
# status, content-type, or JSON envelope. It captures the live contract so the
# new shape becomes the baseline EasyUiEndpointContractTest checks against.
#
# Usage (from repo root): bin/regen-easyui-fixtures
set -euo pipefail

exec podman exec simplerisk-app bash -lc \
  'cd /var/www/simplerisk/tests && ./bin/phpunit --configuration phpunit.xml --testsuite e2e --group regen --testdox'
