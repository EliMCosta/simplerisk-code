{
  "name": "simplerisk-browser-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright browser/UX acceptance suite for SimpleRisk (runs host-side against https://localhost:8443). Guards the DOM/AJAX flows that the curl/PHPUnit e2e suite cannot observe (e.g. the audit-initiation tag-modal Continue button, lazy tree expand, DataTables AJAX load).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0"
  }
}
